<h2>Editing "<%= h @glossary.title %>"</h2>

<% form_for(@glossary) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :title %><br />
    <%= f.text_field :title, :style => "width: 350px;" %>
  </p>
  <p>
    <%= f.label(:language, "From language") %> <br />
    <%= select("glossary", "from_language", Language.find(:all, :order => "name").collect {|l| [l.name, l.id] }) %>
  </p>
  <p>
    <%= f.label(:language, "To language") %> <br />
    <%= select("glossary", "to_language", Language.find(:all, :order => "name").collect {|l| [l.name, l.id] }) %>
  </p>
  <p>
    <%= f.label :description %><br />
    <%= f.text_area :description, :style => "width: 350px; height: 200px;" %>
  </p>
   <p>
	<%= f.check_box :public %>
    <%= f.label "Should it appear in the Catalogue?" %>
   
  </p>
  <p>
	<%= f.check_box :shared %>    
	<%= f.label "Allow others to edit?" %>
    
  </p>
  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Back', glossaries_path %>