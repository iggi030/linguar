<% form_for :catalogue, :url => {:action => :create } do |f| -%>
<p>Select your native language(s) (hold ctrl to select more than one)</p>

 
  <%= select_tag :native_language,
    options_from_collection_for_select(@languages, 'id', 'name'), {
      :multiple => true,
      :size =>8,
      :style => "width:150px;",
      :id => "native_lang"}
    %>
  
  <div><%= submit_tag 'Continue' %></div>
<% end -%>
